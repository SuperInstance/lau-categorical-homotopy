[package]
name = "lau-categorical-homotopy"
version = "0.1.0"
edition = "2021"
description = "Homotopy type theory meets agent systems — paths, homotopies, fundamental groupoids, and higher inductive types as agent communication protocols"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-categorical-homotopy"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
